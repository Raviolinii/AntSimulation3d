using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SupplyAnt : MonoBehaviour
{
    // Master
    AntsMaster _master;

    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // Master
    public void AsignAntsMaster(AntsMaster master) => _master = master;
}
